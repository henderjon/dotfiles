#!/usr/bin/env awk -f

BEGIN{
    cmd = "ls -1 \"$DOTFILES_DIR\"/bin | grep '^git-' | sort"
    while ((cmd | getline line) > 0) {
        if (line !~ /git-dothelp/) {
            print line
        }
    }
    close(cmd)
}
