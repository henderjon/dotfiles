# Git Config Generator – Generated by Tower (https://www.git-tower.com)
# April 15, 2026

[user]
	name = Jon Henderson
	email = henderjon@gmail.com
	signingkey = B64029804B7D23DB!

[core]
	editor = code --wait
	ignorecase = true

[init]
	defaultBranch = develop

[merge]
	tool = vscode

[rebase]
	autoStash = true
	updateRefs = true

[push]
	default = upstream
	autoSetupRemote = true

[pull]
	rebase = true

[fetch]
	prune = true
	prunetags = true

[column]
	ui = auto

[log]
	abbrevCommit = true

[alias]
	undo = reset --soft HEAD~1
	nuke = reset --hard HEAD~1
	diff-main = !git diff $(git merge-base HEAD main)..HEAD
	lg = log --oneline --graph --decorate --all --abbrev-commit
	who = shortlog -s -n --all --no-merges --
	recent = branch --sort=-committerdate --format='%(refname:short) (%(committerdate:relative))' --count=10
	wip = !git add -A && git commit -m 'WIP'
	unstage = restore --staged
	amend = commit --amend --no-edit
	stash-all = stash push --include-untracked
	aliases = config --get-regexp ^alias\\.

[credential]
	helper = osxkeychain

[commit]
	gpgsign = true

[tag]
	gpgsign = true
