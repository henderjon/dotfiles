# MySQL
--------------------------------------------------------------------------------

% mysql -h $HOST --user $USERNAME -p
> use $DATABSE
> show tables;
> describe $TABLE;
> quit

% mysql_config_editor set --login-path=$name -h$hostPath -u$userName -p
% mysql --login-path=$name

### slow query log
`show full processlist;`

# MySQL ON DUPLICATE KEY
--------------------------------------------------------------------------------

INSERT INTO table1 (col1, col2)
SELECT col1, col2 FROM table2
ON DUPLICATE KEY UPDATE col2 = VALUES(col2);

# SQL ORDER BY
--------------------------------------------------------------------------------

ORDER BY CASE column
	WHEN "string1" THEN 1
	WHEN "string2" THEN 2
	ELSE 3
	END

# MYSQL QUOTES
--------------------------------------------------------------------------------

'keyword'		A keyword in single quotes is a string literal.
`keyword`		A keyword in backticks is an identifier.

# SQLITE
--------------------------------------------------------------------------------

> PRAGMA wal_checkpoint(TRUNCATE);
> VACUUM
> .tables
> .schema $TABLE
> .quit
> echo .dump | sqlite3 $FILENAME

# SQLITE QUOTES
--------------------------------------------------------------------------------

'keyword'		A keyword in single quotes is a string literal.
"keyword"		A keyword in double-quotes is an identifier.

